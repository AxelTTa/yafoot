# Server-only (never commit the real one). Used by scripts/sync-worldcup.mjs
SUPABASE_URL=https://zfsgclwyaapgwxjtzvyd.supabase.co
SERVICE_ROLE=
FD_API_KEY=

# Public client analytics config. PostHog project keys are public client keys,
# but keep real values in deployment env, not in source.
EXPO_PUBLIC_POSTHOG_API_KEY=
EXPO_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
# Native session replay needs a custom development/production build; Expo Go does not support it.
EXPO_PUBLIC_POSTHOG_NATIVE_SESSION_REPLAY=0
