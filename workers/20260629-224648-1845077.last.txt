BLOCKED: PostHog code is implemented, committed, and pushed (`c0faf20`), but not deployed because no real PostHog config exists.

Verified:
- `npx tsc --noEmit`
- `npx expo export --platform web`
- `npx expo export --platform ios`

Missing env:
- `EXPO_PUBLIC_POSTHOG_API_KEY`
- `EXPO_PUBLIC_POSTHOG_HOST`

Telegram update sent. Sources used: PostHog React Native docs https://posthog.com/docs/libraries/react-native and Expo PostHog guide https://docs.expo.dev/guides/using-posthog/