PASS. Completed the focused App Store compliance cleanup and pushed commit `be16c94` to `staging`.

Changed:
- Cleaned `README.md` and `HANDOFF.md` reviewer-copy risk.
- Regenerated tracked `dist-staging` from App Store-safe source.
- Wrote report to `workers/20260626-082930-3636747.last.txt`.
- Sent Telegram update successfully.

Verified:
- Targeted risky-term scans clean across app/source, ASC scripts, support/privacy, screenshots, production `dist`, and `dist-staging`.
- Live `/support` and `/privacy` render publicly with no risky terms.
- `npx tsc --noEmit` passed.

Did not run `scripts/deploy.sh` because no app/live source change was needed, and the shared tree still has unrelated worker artifacts that deploy would commit via `git add -A`. Remaining App Store blocker is ASC-side: build `6` still needs upload/attach before resubmission.